//反转数，用到了字符串数组
#include<cmath>
#include<iostream>
#include<iomanip>
using namespace std;
int main()
{
  int a,b,c,p;
  int mid;
  float s;
  cin>>a>>b>>c;
  p=(a+b+c)/2;
  s=sqrt(p*(p-a)*(p-b)*(p-c));
  cout<<fixed<<setprecision(1)<<s<<endl;
  system("pause");
  return 0;
}
